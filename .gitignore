# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env / secrets — never commit (VITE_LLM_KEY etc). .env.example is the template.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# build artifact — vite inlines VITE_ env (incl. VITE_LLM_KEY); regenerate via: npm run build:offline
public/offline-shell.html
public/icons.svg
public/favicon.svg
