# RFQ Buyer-Intelligence — Code Audit To-Do

_2026-07-13 · multi-agent audit of the dashboard app + both n8n workflows (v44 `bi-user-insights-v10x`, v40 `bi-buyer-unified`). Every P0/P1 adversarially re-verified against the code. Audit only — no code changed._

**6 P0 · 23 P1 · 67 P2 · 17 refuted**

## P0 — must fix before launch

### P0.1 `BuyerLedgerView.tsx:137` — data-honesty
**Previous buyer's Befisc/Sign3 external identity (window.__buyerTwin.observed_external, not keyed by GLID) is merged into the next buyer's ledger and also suppresses the next buyer's own external fetch.**

- **Why:** withObservedExternal: `const obs = window.__buyerTwin?.observed_external; ... return already ? raw : [...raw, { observed_external: obs }]` (lines 134-137), applied on every build at line 234 `setLedger(buildLedger(withObservedExternal(raw)))`. The write at line 260 `__buyerTwin = { ...(w.__buyerTwin||{}), observed_external: obs }` has no GLID key and is never cleared on close/mount, and the effect at line 248 short-circuits: `if (w.__buyerTwin?.observed_external) { setExtState('done'); return; }`. Scenario: pull GLID A (runExternal succeeds, stores A's Befisc/Sign3), close, open GLID B whose n8n raw carries no befisc/sign3 element -> the `already` check (line 136) passes and A's age/gender/income/PAN facts are appended to B's raw; buildExternalCard/persona then show person A's PII as buyer B, and line 249's `ledger.facts.some(sourceNode==='befisc'...)` now sees A's merged facts so B's real external is never fetched. V3/V4 replace __buyerTwin wholesale on their own pulls, but the ledger-view->ledger-view path has no reset (verified: only RFQModal V3/V4 clear it, and only when ignoreTwin toggles).
- **Fix:** Store observed_external keyed by GLID (e.g. __buyerTwin.observed_external_glid) and have withObservedExternal + the extState effect ignore it when the stored GLID differs from the current prop; clear it on unmount.

### P0.2 `traceExport.ts:19` — security
**The Langfuse SECRET key (and public key) is inlined into the bundle and every offline HTML, and the bare `import.meta.env` read here pulls ALL VITE_ vars into the client bundle.**

- **Why:** Line 18-19 read `env.VITE_LANGFUSE_SECRET_KEY` / `VITE_LANGFUSE_PUBLIC_KEY` from `import.meta.env`, and line 76 builds `'Basic ' + btoa(`${cfg.publicKey}:${cfg.secretKey}`)` and POSTs it (line 84) — all in browser code. Because it dereferences the bare `import.meta.env` object, Vite serializes every VITE_ var into the chunk. PROVEN: the actual .env VITE_LANGFUSE_SECRET_KEY value appears 1x in dist/assets/index.js and 1x in public/offline-shell.html; VITE_LANGFUSE_PUBLIC_KEY likewise 1x/1x (values never printed). The Langfuse secret key grants read/write to the observability project, which itself stores buyer traces. Failure scenario: any emailed offline HTML (or the deployed bundle) hands a reader both halves of the Basic-auth credential -> they read/tamper with the Langfuse project. This holds even though the exporter is runtime-gated (`isExportEnabled`) — gating controls whether it RUNS, not whether the value is baked in at build time.
- **Fix:** Move Langfuse ingestion behind a server-side endpoint/proxy that injects the Basic auth header from NON-VITE_ env vars; never reference the secret key in browser code. Destructure only the specific non-secret VITE_ vars you need instead of the bare `import.meta.env` object, and rotate the Langfuse secret+public keys since they have shipped.

### P0.3 `RFQ Buyer Insights — v44 (syntax repair · namesake field-guard · no-audio skip) [bi-user-insights-v10x].json:17` — merging
**pan-gate skip branch (no-PAN buyer) never executes pan-gst-parse, leaving DUMB-MERGE input 10 AND gst-discovery-merge input 1 unfed — the same unrun-input condition the calls-empty stub was added to prevent from hanging the barrier.**

- **Why:** pan-compile emits `if (!rows.length) return [{ json: { skip: true, ... } }];` when no PAN resolves, and pan-gate's condition is `={{ !$json.skip && !!$json.pan }}`, so a no-PAN buyer takes pan-gate OUT1. Connections: pan-gate out1 -> s3-pan-advance-parse, s3-pan-gst-parse, s3-udyam-reg (the sibling s3-pan-gst-parse IS covered), but NOT pan-gst-parse. pan-gst-parse is fed ONLY by pan-gst-if[out1] and pan-gst-idfy[out0]; pan-gst-if <- pan-gst-idfy-build <- pan-gate[OUT0] only. Therefore on a no-PAN buyer pan-gst-parse never runs. It feeds DUMB-MERGE input 10 and gst-discovery-merge input 1. The calls-empty node comment states its whole purpose: 'this emits an EMPTY calls source so the merge barrier never hangs' — i.e. an unrun DUMB-MERGE input hangs the barrier in this deployment. Failure scenario: buyer with no PAN anywhere (profile/befisc/sign3 all empty) -> pan-gst-parse unrun -> DUMB-MERGE in10 + gst-discovery-merge in1 never satisfied -> barrier stalls -> final-assemble never fires -> whole pull hangs/times out (and gst-discovery-merge stalling cascades through gst-compile-list -> gst-consensus -> websearch-build/gweb-build/aadhaar-fact). If the merge instead tolerates unrun inputs (n8n-version-dependent), the degraded outcome is still a data-honesty regression: final-assemble's `calls: ... pan_gst_idfy = ref('pan-gst-parse')` returns null (the $() ref throws->catch->null) instead of pan-gst-parse's intended `{summary:{gst_details:[], status:'skipped', _meta:{source:'pan_gst_idfy'...}}}`, and __health reports pan_gst_idfy as ok:true/status:'ok' — inconsistent with sibling s3-pan-gst-parse which emits an honest 'skipped' summary on the very same no-PAN path.
- **Fix:** Add a connection pan-gate[out1] -> pan-gst-parse (mirror the sibling s3-pan-gst-parse wiring) so the IDfy PAN->GST terminal runs and emits its honest 'skipped' summary on the no-PAN path; or add a pan-gst-empty stub feeding both DUMB-MERGE in10 and gst-discovery-merge in1. Then every barrier-terminal is covered on both IF branches, matching the workflow's established no-unrun-input contract.

### P0.4 `RFQ Buyer Unified — v40 (syntax repair · namesake field-guard · no-audio skip) [bi-buyer-unified].json:3045` — sync
**The standalone LLM chain (profile-bundle → profile-llm → profile-parse) is ORPHANED; the webhook response never contains a buyer{} block, so every inferred card attribute is blank on every standalone pull.**

- **Why:** Connections trace: Webhook1 → t0 → cache-gate → mode_router → …sources… → DUMB-MERGE → final-assemble → cache-store → "Respond to Webhook". `grep -c '"node": "profile-bundle"'` = 0 — NOTHING feeds profile-bundle, so profile-bundle/profile-llm/profile-parse never execute. The node that actually answers (final-assemble, its return on the last line) is `{ glid, fetched_at, derived_anchors, source_registry, source_priority, sources, sources_present, sources_absent, requirement_brain, pipeline_timing, total_pull_s, pipeline_health, __health }` — there is NO `buyer` key. Consumer: buyerProfileModel.ts:273-277 `const buyerBlk = obj(obj(rich).buyer); const llm = Object.keys(buyerBlk).length ? buyerBlk : obj(obj(rich).llm_profile); ... if (Object.keys(llm).length) { setL(...) }`. Failure: fetchBuyerUnified returns final-assemble's payload with buyer absent → buyerBlk={} and llm_profile={} → the whole overlay (setL for buyer_maturity/intent/deal_readiness/objective/stage, business_type/b2b_b2c/sub_industry/retail_wholesale/scale/turnover/procurements, all 10 procurement rows, all 3 market rows) is SKIPPED, plus business_story (line 310), products_of_interest LLM rank (line 455), and business_persona headline (line 524) all read undefined. The standalone card renders as a deterministic-only shell (GST/PAN/Udyam/tenure/PII only) — the exact opposite of its stated purpose ('the server LLM fills the fixed attribute set').
- **Fix:** Re-wire so the LLM subgraph is IN the response path: final-assemble → profile-bundle → profile-llm → profile-parse → cache-store → Respond to Webhook (move cache-store to after profile-parse so the cached payload also carries buyer{}). profile-bundle already reads final-assemble's output via $input; it just needs the incoming main connection that is currently missing.

### P0.5 `RFQ Buyer Unified — v40 (syntax repair · namesake field-guard · no-audio skip) [bi-buyer-unified].json:3100` — parsing
**profile-parse double-nests the LLM output: it sets buyer:llm where llm is the full {buyer:{...},needs_input:[...]} object, so response.buyer.<attr> is always undefined even once the chain is wired.**

- **Why:** profile-parse jsCode: `llm = typeof content === 'string' ? JSON.parse(content) : content; ... return [{ json: Object.assign({}, pass, { buyer: llm, __llm_health: {...} }) }];`. The profile-bundle system prompt mandates `Output STRICT JSON only … TWO top-level keys: buyer (an OBJECT keyed by attribute name) and needs_input (an array)`, so content parses to `{ buyer: {business_type:{...},...}, needs_input:[...] }`. Assigning `buyer: llm` yields response.buyer = { buyer:{...}, needs_input:[...] }, i.e. the attribute map is nested one level too deep. parseBuyerProfile then does `obj(rich).buyer` = {buyer,needs_input} and reads buyerBlk['business_type'] etc. → undefined for every attribute → lf() returns null → zero overlay (same blank card as finding #1, this time even after the wiring is fixed). The frontend contract (enrichment.ts:842) explicitly expects flat: `buyer?: Record<string, { value; confidence; reason?; grounded?; sources? }>`.
- **Fix:** Unwrap in profile-parse: `const out = llm && llm.buyer ? llm.buyer : llm; return [{ json: Object.assign({}, pass, { buyer: out, needs_input: (llm && llm.needs_input) || [], __llm_health: {...} }) }];` so response.buyer is the flat attribute map parseBuyerProfile reads.

### P0.6 `RFQ Buyer Unified — v40 (syntax repair · namesake field-guard · no-audio skip) [bi-buyer-unified].json:3045` — sync
**Standalone's ONE LLM node (profile-bundle) is an orphaned root with zero inbound wires, so the endpoint never emits buyer{} — the whole reason the standalone card exists — yet the FE renders it as success.**

- **Why:** Connections scan: profile-bundle has 0 inbound edges (verified: no connection object lists {node:"profile-bundle"}); it only OUTPUTS profile-bundle→profile-llm→profile-parse→"Respond to Webhook" (connections lines 5427-5456). The live trigger path is Webhook1→…→DUMB-MERGE→final-assemble→cache-store→"Respond to Webhook"; final-assemble's return is `return [{ json: { glid, fetched_at, derived_anchors, source_registry, source_priority, sources, sources_present, sources_absent, requirement_brain, pipeline_timing, total_pull_s, pipeline_health, __health } }]` — NO `buyer` key. In n8n, a node with no incoming connection that is not a trigger never executes on a webhook run, so profile-llm/profile-parse are dead and the Respond node fires from cache-store. The FE consumes buyer{}: enrichment.ts:842 `type BuyerUnified = {…; buyer?: Record<string,{value,confidence,reason,grounded,sources}>}`, fetchBuyerUnified (enrichment.ts:856) accepts the object on `('buyer' in x || 'sources' in x)`, and BuyerProfileStandalone.tsx:27-34 sets data on `'sources' in b` then renders `<BuyerProfileCard rich={state.data}>` (line 82). FAILURE: open ?profile=268590579 → n8n returns {glid,sources,…} with no buyer{} → the card shows only deterministic registry facts while every server-LLM-reasoned attribute (business_type/stage/turnover/sourcing/story with confidence/reason) is permanently blank, and the UI banner reads "Rendered via bi-buyer-unified · superfast — pure API" (i.e. reports success).
- **Fix:** In the bi-buyer-unified workflow, insert the LLM branch into the live path: wire final-assemble (or cache-store) → profile-bundle so profile-bundle→profile-llm→profile-parse becomes reachable from Webhook1, and make profile-parse (which merges _pass + buyer:llm) the node feeding "Respond to Webhook" on the fresh path (cache-store then only persists, not responds). Verify the cache-hit path (respond-cached) also returns a payload that carries buyer{}.

## P1 — high priority

### P1.1 `BuyerLedgerView.tsx:279` — data-honesty
**The extract effect fires a real Gemini call on the PREVIOUS buyer's module-cached rich at mount (no glid match check), and while the correct re-extract runs, the retained previous-buyer output renders under the new GLID.**

- **Why:** extractSynth: `const rich = getEnrichmentRich(); if (!rich) return null;` (line 279) never checks rich.glid against the `glid` prop or that `ledger`/`raw` exist. enrichment.ts keeps `let lastRich` module-level (enrichment.ts:616, set at :787), so it survives close/reopen. On mount for GLID B (or the presetLedger RFQ-Ledger view, whose pull effect returns early at line 186), synthCtx is built from GLID A's rich, lastPromptRef is '' (fresh ref, line 163), so the effect at 293-313 fires extractBuyerProfileLLM on A's data immediately — a wasted paid call. When B's pull lands, the effect re-fires with B's prompt but 'RETAIN the previous out' (line 309 `setMsynth((m) => ({ ...m, status: 'loading' }))`) keeps A's out, so for the ~1 min re-extract window rawFinals = extractedToFinals(A.out, B.evidenceIds): A's values pass the curated-row gate `lfA` (line 1220, confidence>=50 only, no grounded check) and the card path `finals.filter((f) => (f.confidence ?? 0) >= 50)` (line 1501), showing buyer A's attribute values on buyer B's card.
- **Fix:** In extractSynth, bail unless the rich belongs to this view: `if (!rich || String((rich as {glid?:unknown}).glid||'') !== glid.trim() ) return null;` (and also `if (presetLedger) return null;`), so the extract only ever runs on this GLID's pull.

### P1.2 `BuyerLedgerView.tsx:204` — data-honesty
**The 'alive' race guard only drops component setState; a stale pull that resolves later still overwrites the module globals (lastRich/lastRaw/lastHealth), so every render-body getEnrichmentRich() consumer — including the BuyerProfileCard rich prop — silently flips to the stale buyer's/pull's data.**

- **Why:** Line 204: `fetchEnrichment(glid,{tier,fresh}).then(({raw}) => { if (alive && raw) { setRaw(raw); ... } })` with the comment (lines 198-201) claiming 'the stale one is dropped'. But enrichment.ts:787-790 sets `lastRich = ...; lastRaw = ...` unconditionally when ANY fetch resolves, and the fetch is never aborted on unmount (timeout-only AbortSignal, enrichment.ts:777). Scenario: start a `normal` pull for GLID A (observed up to ~17 min), close the view mid-pull, open GLID B (superfast resolves in ~1 min); minutes later A's fetch resolves -> lastRich = A's rich; on B's next re-render (any click/toggle) the L1 channels, zero-vs-flake banner (1499), CrawlerBand seed (1550), availability/PII/GST blocks and `<BuyerProfileCard rich={getEnrichmentRich()...}` (1501) all render GLID A's data while the header and ledger/finals still say GLID B — a mixed-buyer screen. Same mechanism applies to a Fresh-pull racing its superseded non-fresh run (Fresh button is not disabled while loading, line 810), which can also re-clobber enrichResultCache (enrichment.ts:802) with the stale result.
- **Fix:** Have fetchEnrichment only commit the module globals when the resolving run is still the latest for that key (compare against a monotonically increasing runId stored per glid:tier, or pass an 'isCurrent' callback), or store rich keyed by glid and make getEnrichmentRich(glid) explicit.

### P1.3 `ledgerBands.tsx:740` — data-honesty
**UC2DebugBand renders a FAILED enrichment LLM call as "No corrections/additions — requirement confirmed clean." because its status union has no 'failed' state and the producer maps a rejected promise to status 'done' with out=null.**

- **Why:** ledgerBands.tsx:740: `: status === 'done' ? <div className="text-[11px] text-gray-400 italic">No corrections/additions — requirement confirmed clean.</div>`. Producer BuyerLedgerView.tsx:411: `.catch(() => setUc2Map((m) => ({ ...m, [idx]: { status: 'done', out: null, usage: null } })));` and call site 1542 passes `edits={r.result?.edits}` where `result = (ctx && entry.out) ? mergeUC2LLM(...) : null`, so a network/parse failure yields edits=undefined → ed=[] → the 'done' branch. Scenario: Gemini times out on uc2Enrich → the per-requirement debug band positively asserts the requirement was verified clean, fabricating a verdict from a failure.
- **Fix:** Add a 'failed' status to UC2DebugBand's status union and render an explicit failure line (rose, 'enrichment call failed — no verdict'); in BuyerLedgerView's catch, set status:'failed' instead of 'done'.

### P1.4 `ledgerBands.tsx:636` — data-honesty
**UC1 offer-enrich 'corrected' and 'added' fields are counted in the always-visible enrich banner but their values/drills are rendered nowhere — L6Band consumes `fields` only to list dropped/suggested rows, and the receipt's claim that reasoning is 'on the card' is false for this path.**

- **Why:** ledgerBands.tsx:636: `const rejected = fields.filter((f) => f.action === 'dropped' || f.action === 'suggested');` is the ONLY consumption of `fields`; line 661 then says 'per-field reasoning is on the card itself — click any struck / violet / green value above', but the card's struck/violet values come exclusively from `uc2` (the separate enrichRequirementLLM path, Requirement tab). BuyerLedgerView.tsx:1127-1130 builds the banner from offerFields: '✓ Enriched · N corrected · M added…'. Scenario: user clicks '▶ enrich (LLM)' in Profile mode → offerEnrichLLM corrects 2 specs → banner announces '2 corrected' yet no value on the card changes and the corrected before→after pairs plus their evidence drills are unreachable in the UI.
- **Fix:** Render applied corrected/added OfferFieldRows (before→after + drill) inside the enrichment run details receipt (or overlay them on the left-column specs), or make the banner link to a section that actually shows them.

### P1.5 `ledgerBands.tsx:545` — data-honesty
**Every 'Available' anchor popover hardcodes a 100% deterministic confidence chip, which directly contradicts the sub-100 confidence printed in the same popover's note for resolved anchors like Name.**

- **Why:** ledgerBands.tsx:545: `<b>{a.label}</b>{confidenceChip(100, false)}` — chip expands to 'deterministic — a single verified source, taken at face value'. Producer BuyerLedgerView.tsx:1298 pushes the Name anchor with `verified: resolvedName.confidence >= 85` and `note: \`${resolvedName.full ? 'full name' : 'first name only'} · ${resolvedName.confidence}% (${resolvedName.source})\``. Scenario: name resolved at 70% from WhatsApp → popover shows a '100%' chip one line above a note reading 'first name only · 70% (WhatsApp)' — two contradictory confidence numbers for the same value; same for a PAN row flagged '⚠ 2 distinct PANs'.
- **Fix:** Pass the anchor's real confidence into the popover (add an optional confidence field to L6Availability, defaulting to 100 only for literally on-file values like mobile/email/GST), or drop the chip for anchors that carry their own confidence in the note.

### P1.6 `ledgerBands.tsx:782` — data-honesty
**CrawlerBand renders LLM-extracted OSINT signals with `confidenceChip(s.confidence, false)`, so expanding the chip mislabels a web-scrape LLM guess as 'deterministic — a single verified source, taken at face value' and suppresses the LLM scoring criteria.**

- **Why:** ledgerBands.tsx:782: `{s.value} {confidenceChip(s.confidence, false)}`. The signals are produced by an LLM: src/lib/osintEnrich.ts:98 `const out = await osintSignalsLLM(seed, results); signals = Array.isArray(out?.signals) ? out.signals : [];` (the band's own header at line 769 says 'LLM extracts specific signals' and brands them LOW-confidence observed-only). Scenario: a Firecrawl result for a possible namesake yields signal {value:'xyz.com', confidence:55}; clicking 55% shows 'how it's scored: deterministic — a single verified source, taken at face value (this is corroboration, not a probability)' — the exact opposite of what the number is.
- **Fix:** Call confidenceChip(s.confidence, true) (optionally threading the model's per-signal reason), or add a third 'llm-observed' mode whose explainer says the score is model-reported over uncorroborated web text.

### P1.7 `BuyerProfileCard.tsx:325` — data-honesty
**The GST Verification Status green-check test `/active|verified/i` substring-matches 'Inactive' and 'Not Verified', rendering a bad GST status as an emerald '✓' success.**

- **Why:** Line 325: `<span className={/active|verified/i.test(String(m.company.gstStatus.value)) ? 'text-emerald-600 font-semibold' : 'text-amber-600 font-semibold'}>{/active|verified/i.test(String(m.company.gstStatus.value)) ? '✓ ' : ''}{m.company.gstStatus.value}</span>`. The value is the literal `gstin_status` from IDfy/consensus (model line 371) — real GSTIN statuses include 'Inactive' and 'Suspended'. 'Inactive' contains 'active' → the card shows '✓ Inactive' in green, i.e. a lapsed registration styled as verified-good.
- **Fix:** Anchor the positive test (`/^(active|verified)\b/i`) and check negatives first (`/inactive|cancel|suspend/i` → amber/rose).

### P1.8 `BuyerProfileCard.tsx:444` — data-honesty
**The card renders a fabricated 'TrustSEAL Buyer Plan → Plan Type: TrustSEAL Verified' row for EVERY buyer, contradicting the model's explicit plan:null contract and the card's own 'NO fabricated data reaches the screen' guarantee.**

- **Why:** Lines 442-447 hardcode `<span ... title="TrustSEAL Verified (sample)">TrustSEAL Verified</span>` with only an 8px italic 'placeholder' note, while buyerProfileModel.ts:593 sets `plan: null, // §2 — no plan_type / activated_on field anywhere in the pipeline → card omits the TrustSEAL Plan tile entirely (never fabricated)` and the card header (line 5) claims 'NO fabricated data reaches the screen'. Failure: an 'Unverified Account' or '⚠ Fraud-flagged account' buyer (line 250-252 badge) simultaneously shows 'Plan Type: TrustSEAL Verified' one column over — a scanner reads a verification claim that exists nowhere in the data.
- **Fix:** Delete the placeholder section (matching the model contract) or render the standard muted 'Not available' empty state instead of a sample value.

### P1.9 `buyerProfileExtract.ts:744` — data-honesty
**buyer_maturity has an explicit default-to-value rule — 'otherwise "established business"' — so a buyer with zero maturity evidence is fabricated as an established business, and business_stage is forced to agree with it.**

- **Why:** Line 744: "Use \"early-stage business\" ONLY when those signals appear; otherwise \"established business\"." and line 745: "business_stage ... MUST agree with buyer_maturity". This is the only attribute in # QUESTIONS whose rule directs a value when evidence is ABSENT (every other attr says OMIT). Concrete failure: a 2-month-old unverified GLID with one BuyLead, no GST, no PNS, no setup signals → the instruction yields buyer_maturity='established business' (and business_stage propagates it) — the exact opposite of the truth and a direct violation of the GOLDEN RULE (line 806) and the owner's missing-input-never-fabricates rule.
- **Fix:** Change the rule to three-way: early-stage on setup signals; 'established business' ONLY on positive establishment evidence (verified-business flag, GST/Udyam vintage, multi-year tenure); otherwise OMIT and add to needs_input.

### P1.10 `gemini.ts:209` — data-honesty
**extractBuyerProfileLLM discards the top-level needs_input array the extract prompt demands, so the LLM's honest 'could not ground, ask the buyer' channel never reaches the UI.**

- **Why:** Line 209: `return { out: Array.isArray(attrs) ? { attributes: attrs as SynthLLMOut['attributes'] } : null, usage };` — only `attributes` is rebuilt from the parsed JSON; `parsed.needs_input` is dropped (same in synthesizeProfileLLMWithUsage line 184). The prompt (buyerProfileExtract.ts:790) demands 'STRICT JSON with TWO top-level arrays — "attributes" and "needs_input"', and BuyerLedgerView.tsx:1316 calls `extractNeedsInput(msynth.out)` where msynth.out is exactly this return value — extractNeedsInput (buyerProfileExtract.ts:926) reads `out.needs_input`, which is now always absent. Failure: the model honestly reports e.g. payment_mode as ungrounded with a best_next_question → gemini.ts silently deletes it → the 'Needs input — ask the buyer' panel (ledgerBands.tsx:602) never renders LLM data and always falls back to the deterministic stillAsk list. The interface even declares `needs_input?` on SynthLLMOut (line 166) but no code path ever populates it.
- **Fix:** In both tolerant parses, carry needs_input through: `return { out: { attributes: attrs, needs_input: Array.isArray(parsed.needs_input) ? parsed.needs_input : undefined }, usage }` (guarding element shape is already done downstream in extractNeedsInput).

### P1.11 `gemini.ts:123` — correctness
**callLLM has no timeout/AbortController, so a hung gateway request never resolves — llmInFlight stays incremented forever and the global 'working…' loader spins permanently with no health-ring record.**

- **Why:** Line 123: `const res = await fetch(ENDPOINT, { method: 'POST', headers: {...}, body: ... });` — no `signal`, no timeout, and no retry anywhere in the file. The finally block (line 158 `llmInFlight = Math.max(0, llmInFlight - 1); emitLLMActivity();`) only runs when the promise settles. Failure: imllm.intermesh.net accepts the connection but never sends a response (stalled proxy/slow-drip) → fetch stays pending indefinitely → llmInFlight never decrements → every onLLMActivity subscriber (the form-wide loader, lines 100-115) shows 'working…' forever, recordLLM never fires for the call, and the buyer/HOD must reload the page. Every one of the ~21 exported LLM functions funnels through this single unguarded fetch.
- **Fix:** Wrap the fetch in an AbortController with a per-call deadline (e.g. 90s default, overridable in LLMOpts); on abort, recordLLM({ok:false, status:0, ...}) and throw so existing catch/fallback paths engage.

### P1.12 `offerEnrich.ts:247` — data-honesty
**mergeOfferLLM applies a 'corrected'/'added' value to an existing field on confidence ALONE — the computed `grounded` flag never gates the action — so an ungrounded/fabricated-citation correction is applied and shown on the L6 card.**

- **Why:** apply() computes `const grounded = !!llm.grounded && ev.length > 0;` (line 244) but the only gate is `if ((action === 'corrected' || action === 'added') && conf < GATE_HI) action = conf >= GATE_MED ? 'suggested' : 'kept';` (line 247) — grounded is not consulted. Line 248 then sets `corrected = ... String(llm.value ...)`. Scenario: LLM returns {field:'title', action:'corrected', value:'Notebook Making Machine', confidence:85, grounded:true, evidence_ids:['f999']} where f999 is not a real fact id. resolveEvidence returns ev=[] and leaks=0 (f999 is in neither buyer nor platform maps, so it is silently dropped, not even counted as a leak), so grounded = true && 0>0 = false. Gate: 85 >= 70 → action stays 'corrected', value applied. BuyerLedgerView.tsx:1109 pushes it as {after:f.corrected, action:'corrected'} → struck old title + violet fabricated title on the card, and the enrich banner (BuyerLedgerView.tsx:1127) counts '✓ Enriched · 1 corrected'. This is exactly the fabrication the grounding rule forbids. Note the SAME function gates the new-field path on grounded (`if (grounded) { specs.push(...) }`, line 267), and uc2Enrichment.ts:185 gates on grounded too — this existing-field path was missed.
- **Fix:** In apply(), require grounded to keep a change: when action is 'corrected'/'added' and !grounded, demote to 'suggested' (or 'kept'), mirroring the new-field guard at line 267 and mergeUC2LLM at uc2Enrichment.ts:185.

### P1.13 `worldEnrichment.ts:184` — correctness
**GST verified flag uses substring `.includes('active')`, so a status of "Inactive" (or "Not Active") is decoded as verified=true.**

- **Why:** `business.verified = business.status?.toLowerCase().includes('active') ?? false;` — 'inactive'.includes('active') is TRUE (the substring 'active' sits at index 2 of 'inactive'), and 'not active'.includes('active') is also TRUE. Scenario: GST-Advance returns status:'Inactive' for a suspended/cancelled-inactive registration → business.verified=true → worldToTwinSignals() (line 230) emits gst_verified:true → the Twin is told the GSTIN is verified/active when it is the opposite. (Module is standalone/not-yet-wired per the header, so impact is latent, but the defect ships the moment it is wired.)
- **Fix:** Match the status exactly, e.g. `business.status?.trim().toLowerCase() === 'active'`, or whitelist the active states — do not use substring includes().

### P1.14 `twinAdapter.ts:122` — drift
**finalsToBuyerProfile reads pre-rename attribute keys (maturity/buyer_stage/supplier_preference/preferred_channel/responsiveness/buyer_persona) that extract-v41 no longer emits, so those BuyerProfile fields are silently always undefined.**

- **Why:** The extract's closed key set (buyerProfileExtract.ts:792) emits `business_persona|buyer_maturity|...|communication|...|preferred_suppliers|business_stage|...` — NOT the legacy names. But finalsToBuyerProfile reads the legacy names: line 121 `persona: v('buyer_persona') || v('business_type')`; line 122 `maturity: v('maturity') || v('buyer_stage')`; line 126 `supplierPreference: v('supplier_preference')`; line 127 `engagement: v('preferred_channel')`; line 128 `responseSensitivity: v('responsiveness')`. `v()` (line 118) returns undefined for a missing key, so under the default EXTRACT_TWIN_ON path profile.maturity / supplierPreference / engagement / responseSensitivity are ALWAYS undefined, and persona degrades to the bare business_type token instead of the rich business_persona headline. Concrete failure: a buyer whose extract yields buyer_maturity='established business' and communication='WhatsApp' → RFQModalV4 buildTwinPlanInput drops the 'Buyer maturity' whyKnown row (line 1164 `bp?.maturity || ''` → hasEv false) and the responseSensitivity-driven 'Purchase urgency' row (line 1172 `/low tolerance|delay/i.test(bp?.responseSensitivity||'')` → always false, despite the line-1170 comment claiming it 'finally CONSUMES responseSensitivity'), and buyerProfileKnown (lines 1010/1012/1015/1016) omits maturity/channel/supplier/response — real extract data lost, not fabricated.
- **Fix:** Rename the reads to the extract-v41 schema: persona→v('business_persona')||v('business_type'), maturity→v('buyer_maturity')||v('business_stage'), supplierPreference→v('preferred_suppliers'), engagement→v('communication'); responsiveness has no extract equivalent, so source responseSensitivity from communication/behavioral evidence or leave it explicitly absent rather than reading a dead key.

### P1.15 `twinAdapter.ts:94` — drift
**finalsToBuyerTwin reads renamed extract keys (preferred_channel/responsiveness/buyer_persona/current_active_intent/industry), so layer_b traits never populate and current_active_intent shows the product list instead of the deduced buyer_intent.**

- **Why:** Against the extract-v41 key set (buyerProfileExtract.ts:792), these reads are dead: line 94 `whatsapp_affinity: /whatsapp/i.test(val('preferred_channel'))...` (real key is `communication`) → always undefined; line 96 `response_sensitivity: trait('responsiveness','LMH')` (no such key) → always undefined; line 97 `decision_style: byKey.has('buyer_persona') ? trait('buyer_persona')` (real key is `business_persona`) → never set; line 88 `secondary_roles: val('industry')...` (real key is `sub_industry`) → always undefined; line 71 `activeIntentVal = val('current_active_intent') || val('products_of_interest') || val('buyer_persona') || val('industry')` — `current_active_intent` and `buyer_intent` are never read, so layer_c.current_active_intent is populated with the products_of_interest list rather than the extract's actual `buyer_intent` value. Concrete failure: a buyer with communication='WhatsApp (High)' and buyer_intent='replacing worn cutting blades' → buyerTwin.layer_b_behavioral.whatsapp_affinity is undefined (RFQModalV4 line 1188 `lb?.whatsapp_affinity?.value` never contributes) and layer_c.current_active_intent shows the product list, not 'replacing worn cutting blades' (RFQModalV4 line 1153 uses it as the twin's history-derived intent fallback).
- **Fix:** Rename to the extract-v41 schema: preferred_channel→communication, buyer_persona→business_persona, industry→sub_industry, and put buyer_intent first in the activeIntentVal chain (val('buyer_intent') || val('products_of_interest') || val('business_persona')); response_sensitivity has no extract key, so derive it from communication or omit rather than reading a nonexistent 'responsiveness'.

### P1.16 `BuyerLedgerView.tsx:1206` — correctness
**A raw NUL byte (U+0000) is embedded in the source of the most bug-dense file, silently making the whole 199 KB file register as binary to grep/git/rg and blinding text-search-based review.**

- **Why:** Verified with python: the file contains exactly one NUL byte at offset 149911 (line 1206). The line reads `... && !norm(operatingCity).includes(norm(idn?.city || '\x00')) && !opIsSourcing)` — the intended space fallback `' '` is actually the byte 0x00. Concrete impact: `rg` reports `binary file matches (found "\0" byte around offset 149911)` and plain `grep -n 'L6Band' BuyerLedgerView.tsx` returns NOTHING (silent) instead of the 2 matches `/usr/bin/grep` finds — any auditor grepping this file without --text gets an empty result and a false 'not present' signal across all ~1900 lines. (The runtime effect — norm('\0')→'' so `.includes('')` is always true, disabling the strike-through location correction — is the already-tracked P2; this finding is the distinct source-integrity/tooling hazard.)
- **Fix:** Replace the NUL byte on line 1206 with a real space (`idn?.city || ' '`), or better `idn?.city || ' -never'`-style sentinel that norm() won't collapse to ''; then guard the whole predicate with `&& !!idn?.city` as buyerDetails.resolveAvailable already does, so an empty city can't make includes('') true.

### P1.17 `BuyerLedgerView.tsx:1312` — data-honesty
**The L6 'GST Verified' ribbon is presence-only: gstVerified is built from decodeGST() without ever checking format validity or registration status, so an invalid or CANCELLED GSTIN gets a green 'GST Verified' badge.**

- **Why:** `const gstVerified = docs.gst ? { gstin: docs.gst.gstin, ... advance: gstAdv || undefined } : null;` — docs.gst comes from decodeGST (src/lib/gstDecode.ts:32-46), which returns a non-null object with `registered: true` for ANY 15-char string and only sets `valid` (never consumed here). ledgerBands.tsx:393-422 then renders `🧾 GST Verified` whenever gstVerified is truthy, with no status gate. Failure scenario: a buyer whose identity.gst is a genuine but de-registered GSTIN → advance.status = 'Cancelled' is shown inside the popover, yet the headline still reads 'GST Verified' (green emerald badge); likewise a malformed 15-char string like '111111111111111' yields valid=false but still 'GST Verified · Sikkim · Unknown'. This contradicts the owner's GST-unknown Golden Rule (task #69) and is a second, distinct instance from the BuyerProfileCard `/active|verified/i` bug (different surface, different mechanism — that one at least reads a status string; this one never looks at status at all).
- **Fix:** Gate gstVerified on `docs.gst.valid` AND on an active status (e.g. only build it when gstAdv?.status is absent or matches an explicit `/^active$/i` after normalization); when the GSTIN is present but not verified-active, render a neutral 'GSTIN on file' chip instead of the emerald 'GST Verified'.

### P1.18 `offlineSnapshot.ts:33` — data-honesty
**The downloaded offline snapshot silently drops the client-fetched Befisc/Sign3 external identity — maybeHydrateOffline never restores window.__buyerTwin, and the capture writes the pre-merge raw — so the offline copy shows fewer verified anchors than the live view it claims to reproduce.**

- **Why:** In BuyerLedgerView the ledger is built from the MERGED array (line 234: `setLedger(buildLedger(withObservedExternal(raw)))`), where withObservedExternal reads `window.__buyerTwin.observed_external` (lines 134-137). But client-fetched external only ever lands on that window global (line 260: `window.__buyerTwin = { ...observed_external: obs }; setRaw(prev => [...prev])` — the raw STATE is only shallow-bumped, never gains observed_external). The download captures `legacy: raw` (line 820) — i.e. the pre-merge raw — and OfflineSnapshot (this file, interface lines 11-26) has no buyerTwin/external field. maybeHydrateOffline (lines 29-38) sets __offlineSnapshot, __ledgerDemoRaw, seedEnrichment, seedLLMRaw but NOT window.__buyerTwin. Failure scenario: buyer whose external identity was fetched client-side (runExternal) → live screen shows External card + Available ✓✓ verified rows + bank-verified name; user clicks Download; opened offline, withObservedExternal(__ledgerDemoRaw) reads undefined → all of that external intelligence is gone, breaking the offline-shell 'works exactly like live' contract for the highest-value paid signal.
- **Fix:** Capture the external layer into the snapshot (add `observedExternal: window.__buyerTwin?.observed_external` to the OfflineSnapshot built at BuyerLedgerView:820) and re-seed it in maybeHydrateOffline (`w.__buyerTwin = { observed_external: snap.observedExternal }`); or capture `legacy: withObservedExternal(raw)` so the merge is baked into snapshot.legacy.

### P1.19 `RFQ Buyer Insights — v44 (syntax repair · namesake field-guard · no-audio skip) [bi-user-insights-v10x].json:104` — parsing
**requirement node's parseLoose control-char stripper uses the wrong regex /[ -]+/g (space + literal hyphen) instead of /[ -]+/g, so it (a) fails to strip the control chars it was meant to and (b) corrupts hyphens in MCAT category names.**

- **Why:** In node `requirement`, jsCode line 104: `const parseLoose=s=>{ if(s&&typeof s==='object') return s; try{ return JSON.parse(String(s).replace(/[ -]+/g,' ')); }catch(e){ return null; } };`. Verified byte-for-byte: the character class is codepoints 91,32,45,93 = `[`,space,`-`,`]` (hyphen last => literal), i.e. it replaces runs of spaces and hyphens with a single space. The three sibling decoders csl-enrich-city.js:6, csl-enrich-mcat.js:6, csl-enrich-prod.js:6 all correctly use `.replace(/[ -]+/g,' ')` to strip control chars (parse_rows.js:7 documents this: raw control chars 'crash strict JSON parsing (hit live on mcat 13467)'). parseLoose is invoked at line 120 in the async Redash job-poll fallback of resolveMcats (the sync cache path at line 117 skips it). Two concrete failure modes when the q12158 MCAT-name lookup returns a job (cold/expired 30-day cache): (1) if the Redash JSON payload contains control characters, JSON.parse throws -> parseLoose returns null -> r stays undefined -> rows=[] -> mcatMap empty -> EVERY buylead's `category` resolves to null (silent category loss); (2) if no control chars but names contain hyphens, JSON still parses but values are mangled -> e.g. 'e-Rickshaw' becomes 'e Rickshaw', 'T-Shirt' becomes 'T Shirt', 'Ready-Made-Garments' becomes 'Ready Made Garments'. The corrupted/lost category name is then written to each requirement (jsCode line 187 `category = category_id ? (mcatMap[category_id] || null) : null`) and reaches both the extract LLM and the buyer card presented as truth.
- **Fix:** Replace the regex in requirement node's parseLoose (jsCode line 104) with the same control-char stripper the sibling decoders use: `String(s).replace(/[ -]+/g,' ')` (or the more precise `/[ --]/g` that parse_rows uses). This restores control-char stripping (the intended behavior) and stops destroying hyphens inside category-name values.

### P1.20 `RFQ Buyer Unified — v40 (syntax repair · namesake field-guard · no-audio skip) [bi-buyer-unified].json:3592` — correctness
**Standalone result-cache is permanently dead: cache-store always writes key tier 'full' while t0 always reads a normalized tier (superfast/fast/normal), so the write key can never match the read key — every standalone pull re-runs all paid externals.**

- **Why:** cache-store computes `let __t0tier = null; ... __t0tier = t0j.__t0 && t0j.__t0.tier; const tier = String(__t0tier || (j0.pipeline_mode && j0.pipeline_mode.tier) || j0.tier || 'full');`. t0 sets `__t0: Date.now()` (a NUMBER), so `t0j.__t0.tier` is undefined -> __t0tier is undefined. The standalone `final-assemble` (line 307) return object has NO `pipeline_mode` and NO `tier` (grep: tier count 0, pipeline_mode count 0), so `j0.pipeline_mode.tier` and `j0.tier` are also undefined -> tier falls to the literal default `'full'`. Meanwhile t0's READ key uses its resolved `tier` var: `tier = (q.full==='1'||q.tier==='full')?'normal':(q.fast==='1'?'fast':'superfast')` and reads `sd.__extcache['res:'+glid+':'+tier]` — i.e. 'normal'/'fast'/'superfast', NEVER 'full'. Simulated: READ `res:GLID:normal` vs WRITE `res:GLID:full` => MATCH false. Result: the 24h cache stores entries under a key nothing ever reads; the dashboard v42 comment on the identical node warns this exact divergence 'made the cache permanently miss (all paid APIs re-ran every pull)'. The dashboard is saved only because ITS final-assemble emits pipeline_mode.tier; the standalone lost that carrier.
- **Fix:** Make the standalone write key equal the read key: either have standalone final-assemble emit `tier` (or `pipeline_mode.tier`) equal to t0's resolved tier, OR in cache-store read the tier directly from t0's query (`$('t0').first().json.tier`) instead of the dead `t0j.__t0.tier`/`j0.pipeline_mode.tier`/`'full'` fallback chain. Also fix the shared root cause: t0 stores __t0 as a bare timestamp, so `t0j.__t0.tier` is always undefined in BOTH workflows (masked in the dashboard, fatal here).

### P1.21 `RFQ Buyer Insights — v44 (syntax repair · namesake field-guard · no-audio skip) [bi-user-insights-v10x].json:1302` — polling
**On the slowest tiers the synchronous webhook (responseMode='responseNode') holds the client connection until the entire pipeline finishes; the Parallel deep-web poll branch alone is bounded at ~13min (up to ~27min with poll retries) and there is no workflow executionTimeout — this exceeds the stated ~8min budget and typical proxy limits, so the caller 504s while n8n keeps running.**

- **Why:** Webhook1 responseMode='responseNode' (dashboard line 1302; standalone line 969). Main pull responds only at 'Respond to Webhook', reached via DUMB-MERGE (22-input barrier that includes websearch-parse in17) -> final-assemble -> cache-store -> Respond. websearch-guard caps the Parallel loop at `if ($runIndex > 8) return {__timeout:true}` = 9 poll iterations (line 3296), and there is NO Wait node in the loop (guard-if false -> websearch-poll). websearch-poll has timeout 90000 AND retryOnFail:true maxTries:2 waitBetweenTries:1000, so a 90s long-poll timeout burns a retry -> up to ~180s per iteration -> worst case 9x180s ≈ 27min (single-poll path ≈ 9x90s ≈ 13.5min). settings has only `executionOrder:v1` / `binaryMode:separate` — no executionTimeout (line 6580). Fast tier is also exposed: calls/pns transcription branches each poll ~90s then hit calls-transcribe/pns-transcribe (timeout 120000, maxTries 2 => up to 240s) and DUMB-MERGE waits for them. There is no early/partial responder on the main pull (only respond-cached and the build-ack respond immediately). Same topology in standalone (websearch-guard line 2963).
- **Fix:** Bound the synchronous run to under the proxy limit: add an overall executionTimeout in settings and/or an early 'facts-ready' Respond that returns before the slow web/transcription branches (async-continue for the rest), and lower the Parallel exposure (fewer guard iterations and/or single 90s poll with no retry so an iteration can't double to 180s). At minimum align the guard/poll math so worst-case Parallel duration is under the stated 8min budget and the gateway timeout.

### P1.22 `BuyerLedgerView.tsx:411` — data-honesty
**A failed requirement-enrichment (UC2) LLM call is collapsed into status 'done' with null output, so the UC2 debug band affirmatively reports the requirement was checked and is clean when the check never completed.**

- **Why:** Line 409-411: `enrichRequirementLLM(p.system, p.user).then(...).catch(() => setUc2Map((m) => ({ ...m, [idx]: { status: 'done', out: null, usage: null } })))` — the catch sets status:'done' (there is no 'error' status). In ledgerBands.tsx UC2DebugBand the terminal render is `} else status === 'done' ? <div ...>No corrections/additions — requirement confirmed clean.</div>` (line ~740) and `applied.length === 0 && status === 'done'` → 'No changes applied … The base requirement stands.' (line ~732). FAILURE: a network/parse error in enrichRequirementLLM (or a queued item) yields status 'done' + out null → uc2Result null → zero edits → the reviewer is told the requirement was 'confirmed clean' though the enrichment never ran. Owner rule: a missing/failed input must read as no_data, never as a positive result.
- **Fix:** Add a distinct 'error' status on the .catch for both UC2 (line 411) and offer enrichment (line 341), thread it into UC2DebugBand / the enrich banner, and render an explicit 'enrichment failed — retry' state (never 'confirmed clean' / '✓ Enriched') when the LLM call did not succeed.

### P1.23 `BuyerLedgerView.tsx:341` — data-honesty
**A failed offer-enrichment LLM call is collapsed into status 'done', and the enrich banner then renders '✓ Enriched · nothing to correct … (N fields verified)', presenting an errored (or never-run) enrichment as a successful clean pass.**

- **Why:** Line 341: `offerEnrichLLM(p.system, p.user).then((out) => setOfferLLM({ status: 'done', out })).catch(() => setOfferLLM({ status: 'done', out: null }))`. Line 343: on out null `offerResult = offerSkeleton.result` (deterministic skeleton). The banner (lines 1127-1131) computes changed counts from offerFields and, when the LLM produced nothing, shows `✓ Enriched · nothing to correct — the recorded lead already matches the buyer's own words (${k} fields verified)`. FAILURE: offerEnrichLLM rejects (or requirements.length===0 so the effect at 336 early-returns leaving offerLLM idle) → user sees a green '✓ Enriched' success though no LLM enrichment occurred. Contrast with msynth, which correctly distinguishes an 'error' state.
- **Fix:** Give offerLLM an 'error' status on the catch and render a failed/retry state in enrichControl instead of the success banner; only show '✓ Enriched · nothing to correct' when offerLLM.status==='done' AND offerLLM.out is non-null.

## P2 (critic-flagged)

### P2.1 `requirements.ts:92` — correctness
**The retailLead classifier substring-matches 'personal'/'individual'/'home use' against the product name, so a bulk B2B product flips the 'This might be a retail lead' (B2C/end-user) flag.**

- **Why:** `const retailLead = /end[\s-]?user|individual|personal|\bretail\b|b2c|home use|household/i.test(`${buyerInfo || ''} ${requirementType || ''} ${str(o.product_or_service)}`);` — 'personal' and 'individual' are plain alternations (no word boundary) tested against product_or_service. Failure scenario: a business buying 'Personal Protective Equipment' or an 'Individual Quick Freezing' machine → the substring 'personal'/'individual' matches → retailLead=true → ledgerBands.tsx:622 renders the amber 'This might be a retail lead' end-user hint for a clear B2B bulk buyer.
- **Fix:** Tighten to whole-word, buyer-context tokens (`\b(end[\s-]?user|individual buyer|personal use|home use|household|b2c)\b`) and evaluate it over buyerInfo/requirementType only, not over the product/category name where 'personal'/'individual' are legitimate product descriptors.

### P2.2 `buyerProfileModel.ts:369` — drift
**Integration question (dashboard surface): the dashboard feeds the v10x pull into BuyerProfileCard/parseBuyerProfile, whose registry reads target the STANDALONE bi-buyer-unified source keys — so the same buyer's GST/KYB can render on the L6 buylead card but be 'Not available' in the card view.**

- **Why:** BuyerLedgerView:1501 renders `<BuyerProfileCard rich={getEnrichmentRich()...}>` where getEnrichmentRich() is the N8N_HOOK='bi-user-insights-v10x' shape (src/lib/api.ts:19). parseBuyerProfile reads KYB from `cert0`=gst_cert_idfy, `gd0`=gst_detail_union, `gstinUnion`=gstin_union, and company_reg/buyerprofile (buyerProfileModel.ts:361-414, e.g. line 369 `str(cert0.gstin) || str(gd0.gstin) || str(gstinUnion.primary)`). The SAME file's L6 path instead reads `summaryOf(rich,'gst').advance` first (buyerDetails.gstAdvance, src/lib/buyerDetails.ts:304-329). These two consumers of one dashboard rich read GST/company from DIFFERENT source keys. The per-file audits can't see this: it needs a live v10x pull to confirm whether bi-user-insights-v10x actually emits gst_cert_idfy/gstin_union/company_reg/buyerprofile — if it does not (they were built for bi-buyer-unified), the dashboard card-view Company Details/social/PAN block renders empty while the L6 GST ribbon on the same buyer shows the GSTIN. (Related, already-tracked: deal_readiness setL wired but the standalone prompt never emits it — the model has drifted from even its own webhook.)
- **Fix:** Verify on a live 268590579 v10x pull which source keys are present, and either make parseBuyerProfile fall back to the buyerDetails source keys (sources.gst.advance / identity / external) when the bi-buyer-unified keys are absent, or feed the dashboard card from the same buyerDetails-derived model the L6 band uses so both views agree per buyer.

## P2 — cleanup / hardening pool

- `BuyerLedgerView.tsx:1499` [data-honesty] — The zero-vs-flake banner keys off n8n counter fields defaulted to 0 instead of the in-scope parsed requirements list, so it can proclaim '0 BuyLeads returned this pull' directly above a screen that is rendering requirements.
- `BuyerLedgerView.tsx:1437` [drift] — Each UC2 debug block's 'input' prompt is recomputed at render time from the CURRENT context instead of the prompt actually sent, so after a web upgrade or re-extract the band shows a prompt that never hit the LLM next to the real rawOutput.
- `BuyerLedgerView.tsx:944` [correctness] — The PNS-calls 'extracted' count fallback still filters status==='transcribed', which the v18 Go-schema migration replaced with 'extracted', so the label shows '0 extracted' when call_count is absent despite extractions rendering below.
- `BuyerLedgerView.tsx:1206` [correctness] — The location-correction guard uses norm(idn?.city || ' ') which normalizes to '' and String.includes('') is always true, so the strike-through correction is silently disabled for every buyer whose identity has no city (state-only recordedLoc).
- `BuyerLedgerView.tsx:1575` [ux-consistency] — The in-flight LLM toaster reads only the SELECTED requirement's uc2 entry, so switching the offer picker mid-enrichment hides the toaster while a UC2 call is still running (and the queued one shows no indicator either).
- `BuyerLedgerView.tsx:838` [ux-consistency] — During a re-pull (Fresh pull or tier switch) the StagedLoader and the full stale ledger view render simultaneously — the screen splits between 'Pulling buyer X…' and the old pull's complete card with no stale marker.
- `BuyerLedgerView.tsx:229` [dead-code] — fullPending can never become true — it is only ever set to false — so the BuyerProfileCard 'still enriching' pending badge is dead and the comment's claim that it gates the Download button is stale.
- `ledgerBands.tsx:490` [data-honesty] — The specsStatus explanations — including the '⚠ getisq5 returned NOTHING this pull' flake warning — are suppressed whenever the lead has buyerInfo or commercials, turning a specs-fetch flake into a silent blank.
- `ledgerBands.tsx:549` [data-honesty] — In Original mode the static Available icons expose the full unmasked anchor value (mobile, email, PAN, GST) in a hover `title` tooltip, bypassing the click-to-reveal gate that the PII block enforces.
- `ledgerBands.tsx:461` [dead-code] — The Requirement-mode location-correction line (`on && uc2?.location`) is unreachable: the O28 location lock guarantees mergeUC2LLM never emits an applied corrected location, and the dummy builder never sets one.
- `ledgerBands.tsx:394` [ux-consistency] — The GST Verified ribbon popover is a plain <details> outside the name="avail" exclusive-accordion group, so it stays open while Available/Footprint/device popovers open and its w-80 max-h-[28rem] panel (z-20) physically overlaps them in the same right-column area.
- `ledgerBands.tsx:344` [dead-code] — L6Requirement's expiry/status/isExpired/recencyDays are populated by the producer but never rendered by L6Band, so an expired lead is indistinguishable from an active one on the card in Original and Profile modes.
- `BuyerLedgerView.tsx:1403` [dead-code] — The dummy-UC2 call still passes addSpecs and derivedCount, but buildUC2Enrichment (O27 pure-LLM contract) ignores every input except specs, so both arguments (and the UC2Input.altLocation field) are dead.
- `BuyerProfileCard.tsx:257` [data-honesty] — The persona prop renders as the hero headline with no junk-value or confidence gate, bypassing both filters that the same LLM key gets on every other path.
- `BuyerProfileCard.tsx:143` [ux-consistency] — IdentityPanel's same-person verdict (one-directional token containment) disagrees with the model's conflict flag (symmetric substring containment), so the panel can show a green 'likely the same person ✓' chip inside an amber conflict-styled box, or a family-member warning in a no-conflict gray box.
- `buyerProfileModel.ts:81` [data-honesty] — triangulateAddress grants the ✓✓ 'Confirmed by 2 independent sources' marker on a 24-char prefix containment, so a city-only web_osint address 'agrees' with any full GST address in that city.
- `buyerProfileModel.ts:400` [data-honesty] — Both '(Last 6 Months)' surfaces (activity tiles and requirement chart/total) count and chart ALL requirements in the payload — no 6-month window is enforced anywhere.
- `buyerProfileModel.ts:398` [correctness] — The requirement-activity chart's month axis follows the requirement sort order (active-first, then recency), not chronology, so mixed active/expired histories render a jumbled month sequence.
- `buyerProfileModel.ts:214` [data-honesty] — `reqs.length || null` conflates a known ZERO (requirement node succeeded, no BuyLeads) with unknown, rendering '—' where an honest 0 belongs.
- `buyerProfileModel.ts:469` [dead-code] — latestRequirement, hasActiveRequirement, and proofs are computed on every parse (including the proof namesake-suppression pipeline) but have ZERO consumers anywhere in the repo.
- `buyerProfileModel.ts:279` [drift] — The setL overlay wires 'deal_readiness' but the standalone's bi-buyer-unified prompt (newest v40) never emits that key, so Deal Readiness (and the readiness half of the merged Buyer Intent row) is permanently absent on the standalone surface — the 'mirrored' prompts have drifted.
- `buyerProfileModel.ts:565` [data-honesty] — header.company sourced from the GST certificate is stamped provenance:'inferred' (inferred:true), mislabelling registry-grade data as LLM synthesis for any provenance consumer.
- `BuyerProfileCard.tsx:426` [ux-consistency] — Sign3 platforms outside the 4-item CONSUMER list and the 4 social rows render NOWHERE, yet `!m.socialPlatforms.length` in the empty-state guard suppresses the 'No web / social / registry footprint detected' message — a buyer with only e.g. a PAYTM/WHATSAPP presence gets a Digital Footprint section that is a title over nothing.
- `BuyerProfileStandalone.tsx:3` [drift] — The file's contract comment says 'FAST-ONLY … there is no run full' and 'Web OSINT + Udyam are gated OFF at the fast tier', directly contradicted by the component's own 3-tier toggle whose LOWEST tier is documented 10 lines later as including 'Udyam + gweb'.
- `sourceConsumption.ts:71` [data-honesty] — present() effectively returns true whenever the source key exists: the `nonEmpty(node)` fallback fires on the standard {summary, raw} wrapper and scaffolding keys count as content, so the matrix reports 'present' for registries that returned nothing.
- `buyerProfileExtract.ts:517` [data-honesty] — The v41 per-field namesake ⚠ marker (nsk) is applied only to business_type/industry/official_address/website — web_scale (employee_count/turnover_estimate/year_established), web_udyam, web_digital, web_people and web_news lines omit it even when those fields are namesake-flagged.
- `sourceConsumption.ts:116` [correctness] — deriveConsumption's isUsed substring/regex matching double-counts a single cited source: citing 'Befisc GST' (a legal closed-catalog name) marks BOTH the GST rung (Primary) and the External (Sign3/Befisc) rung (Corroborative) as used.
- `buyerProfileExtract.ts:731` [drift] — The pre-v33 generic conflict-priority line ranks Requirement(BuyLeads/ISQ) BELOW CSL, contradicting every injected frozen policy that includes both (IDENTITY/PROCUREMENT/BEHAVIOUR/INTENT all put Requirements above CSL) and the requirements-dynamic rule that makes a repeated RFQ pattern Primary.
- `attributeRulebook.ts:59` [drift] — The frozen-architecture doc comment says ATTR_POLICY covers 'The 32 emitted attributes' but the map (and the prompt's key enum, which it does match 1:1) contains 35 — a stale count in the exact place an auditor checks for enum/rulebook drift.
- `gemini.ts:348` [correctness] — voiceToSpecs (maxTokens 2048) and analyzeImage (maxTokens 1024) keep tight output caps that can clip the JSON mid-transcript, losing the entire extraction — contradicting the file's own raise-to-16000 rationale.
- `gemini.ts:251` [data-honesty] — osintSignalsLLM does not enforce the URL-grounding its prompt promises — signals with an empty or fabricated source_url pass the filter and flow unfiltered into the OSINT band.
- `gemini.ts:63` [drift] — PROMPT_VER stamps uc2Enrich as 'uc2Enrich.v9' while uc2Enrichment.ts is at UC2_PROMPT_VERSION 'uc2Enrich.v10', despite the inline 'MUST mirror' contract — every uc2Enrich health/raw-IO record carries the wrong prompt version.
- `gemini.ts:1067` [drift] — Enum-constrained classification calls (deriveBuyerProfile, classifyFieldTypes, deriveIntent) run at the gateway's default temperature, contradicting the file's own F1/F2 rule of low temp on classification for consistent labels.
- `RFQModalV3.tsx:2641` [ux-consistency] — The no-key toast tells the user to set VITE_GEMINI_API_KEY, but the client actually reads VITE_LLM_KEY — following the instruction leaves voice extraction dead.
- `gemini.ts:146` [ux-consistency] — The LLM_RAW debug capture stringifies multimodal message content to '[object Object],[object Object]', so the L4 'nothing hidden' raw-IO ledger shows garbage for voiceToSpecs/analyzeImage/explainSpec-with-image calls.
- `gemini.ts:126` [redundancy] — callLLM never gates on hasGeminiKey, so the ~15 exported functions that (unlike the 6 guarded ones) skip the key check fire a doomed HTTP request with 'Authorization: Bearer undefined' when the key is unset, polluting the health ring.
- `enrichment.ts:809` [correctness] — The in-flight dedup entry is deleted unconditionally on settle, so a fresh pull that overwrote the map entry (line 752 skips the in-flight check when _fresh) gets deregistered by the OLDER run's finally — later callers miss both cache and in-flight and fire a duplicate multi-minute n8n execution.
- `enrichment.ts:631` [correctness] — isNewUserInsightsShape accepts `sources: null` (typeof null === 'object'), making normalizeNewUserInsights throw on `s[k]` — the whole pull is then swallowed to {profile:null, raw:null} even though the HTTP response arrived with usable top-level data.
- `enrichment.ts:629` [dead-code] — lastAnchors / getEnrichmentAnchors is write-only dead code — the getter has zero consumers in the repo, and the value is also excluded from the per-tier cache restore so it would serve the wrong pull's anchors if ever wired.
- `gemini.ts:63` [drift] — UC2 prompt-version tag in gemini.ts is stale (uc2Enrich.v9) while the lib's UC2_PROMPT_VERSION is v10, so every v10 run is logged/traced as v9.
- `uc2Enrichment.ts:185` [drift] — UC2's confidence gate is NOT actually 'ported from offerEnrich' as the header claims — it applies corrections at conf≥50 whereas offerEnrich only applies at ≥70 (50-69 there is 'suggested', not applied), so UC2 overwrites buyer-recorded fields on medium confidence.
- `uc2Enrichment.ts:205` [correctness] — A spec edit marked kind:'corrected' whose field key doesn't norm-match any base spec key is counted in `corrected`/the summary but is never rendered in specOut — the 'N corrected' banner overstates visible corrections.
- `worldEnrichment.ts:182` [correctness] — When GST-Advance returns hsn_codes but no sac_codes, HSN (goods) codes are stored into business.sacCodes (services) and never reach business.hsnCodes, mislabeling goods codes as service codes.
- `gemini.ts:63` [drift] — gemini.ts self-reports the UC2 prompt version as uc2Enrich.v9 while the actual constant is uc2Enrich.v10, so all UC2 telemetry/eval-over-time records the wrong version.
- `buyerProfileModel.ts:279` [ux-consistency] — decision_maker is extracted and rendered on the BuyLead card but is never wired into the GLADMIN model, despite the code comment stating it must appear on BOTH surfaces.
- `BuyerProfileCard.tsx:84` [ux-consistency] — The two surfaces' Digital-Footprint chips key off different platform allow-lists, so identical Sign3 social_platforms data yields different chips on the BuyLead card vs GLADMIN.
- `buyerProfileModel.ts:276` [ux-consistency] — The >=50 confidence gate for curated LLM rows is enforced by only one of the two BuyerProfileCard callers, so the standalone card shows sub-50 attributes the dashboard hides.
- `BuyerLedgerView.tsx:1537` [drift] — L0Band falls back to a hardcoded promptVersion 'extract-v9' (32 versions stale) when the extract IO is missing, feeding a wrong version into the drift-by-version eval log.
- `buyerProfileModel.ts:289` [ux-consistency] — Selling Channel is surfaced with a deterministic 'Sells on IndiaMART' fallback on GLADMIN but is deliberately excluded on the BuyLead card, so the same field renders on one surface and is hidden on the other.
- `threeBrainRegistry.ts:174` [dead-code] — The three-brain alignment engine (alignBrains + buyerBrainFromFinals + categoryBrainFromIntel + rfqBrainFromState) is never imported by any app surface — only the test script references it; only stateFromFrequency is actually consumed.
- `downloadProfile.ts:113` [security] — Full buyer PII (Aadhaar/PAN/income and the entire enrichment pull) is embedded verbatim into the downloadable HTML that gets emailed around — accepted per the owner's 'not PII-safe' stance, flagged informational.
- `synthtest.mjs:82` [dead-code] — synthtest's 16 assertions exhaustively test the arithmetic “Merged Synthesis Engine”, but that path is explicitly UNHOOKED in source — the suite is green coverage of dead code, not the live extract flow.
- `reqtest.mjs:8` [drift] — reqtest's NOTE_KEY mirror still routes “I am interested in” to buyer notes, but source requirements.ts dropped it so that phrase now stays a spec — stale mirror + an unprotected behavior branch.
- `buyerProfileModel.ts:56` [test-coverage] — The deterministic data layer behind the CURRENT GLADMIN Buyer Profile Card and standalone card (reconcileField conflict panels, triangulateAddress same-source agreement, monthBucket footprint buckets, parseBuyerProfile) has no harness at all.
- `downloadProfile.ts:118` [test-coverage] — The offline-download gating/assembly path (downloadProfileHtml + offline-shell integrity guard) has no harness, despite a history of the offline bundle silently breaking.
- `RFQ Buyer Insights — v44 (syntax repair · namesake field-guard · no-audio skip) [bi-user-insights-v10x].json:172` [data-honesty] — final-assemble __health rollup reports the transcription sources (calls, pns_calls) as ok:true/status:'ok'/count:null even when tier-skipped, empty, or unrun — contradicting sources_absent — because their assemble/empty nodes emit no __health and the rollup never reads summary.call_count.
- `RFQ Buyer Insights — v44 (syntax repair · namesake field-guard · no-audio skip) [bi-user-insights-v10x].json:3060` [polling] — Non-idempotent create-POSTs (websearch-post → Parallel run, gst-cert-post → IDfy async task) have retryOnFail with maxTries 3; on a slow/timed-out response where the server already created the resource, the retry creates a duplicate run/task, wasting vendor credits (only the first run_id/request_id is used downstream).
- `RFQ Buyer Insights — v44 (syntax repair · namesake field-guard · no-audio skip) [bi-user-insights-v10x].json:3970` [data-honesty] — cache-store writes the final payload to the 24h cache unconditionally, with no check on pipeline_health/__health — so a fresh partial/flaked run (PNS/websearch/IDfy/calls timeout or error) is cached and served to the next ≤20 pulls of that GLID+tier; the tracked 'stale flaked run' mitigation is an age-based banner that does NOT fire on a run cached seconds ago.
- `buyerProfileModel.ts:281` [cta] — The standalone prompt emits 5 attributes the standalone card never consumes (decision_maker, urgency, communication, identity_confidence, digital_footprint) — wasted tokens, and urgency being dropped leaves Delivery Timeline blank for the common no-explicit-timeframe buyer.
- `BuyerProfileStandalone.tsx:4` [drift] — Stale tier documentation: the header comment claims FAST-ONLY (no 'run full') and that Web-OSINT + Udyam are gated off at fast, but the component ships a 3-tier toggle and t0 keeps Udyam + gweb on at every tier.
- `RFQ Buyer Unified — v40 (syntax repair · namesake field-guard · no-audio skip) [bi-buyer-unified].json:3584` [perf] — Standalone cache-store writes the result-cache under tier 'full' while t0 reads it under the real tier (superfast/fast/normal), so the 24h result-cache never hits and every ?profile pull re-runs all paid external APIs.
- `enrichment.ts:698` [data-honesty] — The FE discards n8n's authoritative honesty fields (sources_present, sources_absent, pipeline_mode.skipped_for_tier, pipeline_health) and re-derives per-node health from the raw __health array, so tier-skipped transcript sources (calls / pns_calls in superfast & fast) render in the health matrix as ok/'no data' instead of 'skipped for tier'.
- `BuyerLedgerView.tsx:1122` [cta] — The 'enrich (LLM)' button is a one-way switch with no retry: it disappears the moment enrichMode flips to 'offer', enrichMode is never reset on GLID/requirement change, so after an error there is no re-click affordance and later buyers auto-enrich silently.
- `BuyerLedgerView.tsx:817` [cta] — The Download button can stay permanently disabled with a misleading '⏳ AI extract running…' label: msynth resets to 'idle' on GLID and the extract effect early-returns while synthCtx is null, so if the extract context never builds the button never re-enables.
- `OfflineDashboard.tsx:12` [cta] — Inside the downloaded offline shell several header CTAs render as live but do nothing useful: ✕ close is a no-op, Fresh-pull and the tier chips are inert (the pull effect early-returns on __ledgerDemoRaw), and 'standalone ↗' re-opens the offline dashboard rather than the standalone card.
- `MainApp.tsx:164` [cta] — Without ?debug the landing page has zero interactive CTAs — the GLID input + entry button are gated behind isDebug() and every quote button behind SHOW_QUOTE_CTAS=false — so the 'Drop a buyer's GLID' page is a dead-end for a non-debug visitor.
- `BuyerLedgerView.tsx:784` [cta] — CTA coverage inventory — every CTA in the cluster was traced to its effect; the ones below verified OK (handlers are null-safe, toggles round-trip both directions, links carry rel, Fresh-pull busts all three caches).

## Checked & dismissed (verifier refuted)

- `BuyerLedgerView.tsx` — The Download gate enables on msynth.status==='error' with out=null, baking extractOut:null into the offline copy — the silently-blank AI layer the gate's own co → _Code is quoted accurately, but the P1 "silently-blank" framing is refuted on three grounds. (1) Enabling on 'error' is documented, deliberate intent — the gate _
- `BuyerLedgerView.tsx` — The GLADMIN card mount falls back to lastBuyerBlockRef, which is never keyed by or cleared on GLID change, so the PREVIOUS buyer's LLM attributes render on the  → _The line-1501 code and unreset lastBuyerBlockRef exist exactly as quoted, but the cross-buyer-leak is unreachable: BuyerLedgerView is a fixed inset-0 z-50 modal_
- `buyerProfileExtract.ts` — composeGst relabels Befisc's gross_total_income (an income-tax field) as 'turnover' inside the 'GST registration (verified KYB)' evidence line, feeding the LLM  → _Code exists verbatim (buyerProfileExtract.ts:358-359, mirrored at buyerDetails.ts:350), but the finding duplicates the owner-tracked OPEN redundancy "turnover-m_
- `buyerProfileExtract.ts` — The v38 gap-4 'trust anchored Parallel despite Gemini match_confidence=none' carve-out is self-defeating: the Parallel fields pass the withhold gate but compose → _The code trace reproduces (gate at src/lib/buyerProfileExtract.ts:646 lets anchored+mc='none'+parallelHit through, then composeWebOsint line 505 fires the match_
- `buyerProfileExtract.ts` — The output contract's closed `sources` catalog is frozen at the v11 era (8 names) and omits every source added since — Udyam, IDfy, Web OSINT, GST 3-vendor cons → _Refuted: the quoted stale 8-name catalog comment exists (line 800, introduced v11), but the claimed deterministic failure is not reproducible from the code. Not_
- `buyerProfileExtract.ts` — Several per-attribute 'Preferred sources' orders contradict the frozen policy order injected via buildPolicySection for the same attribute — sharpest for annual → _Refuted: the debug banner (BuyerLedgerView.tsx:655-667) only warns on a policy-NAME mismatch (usedPolicy !== rb.policyId), never on winningSource-vs-order, so i_
- `sourceConsumption.ts` — The v38 sources company_reg and buyerprofile are missing from both SURFACES and LABEL_MATCH, so the source-consumption matrix and the per-attribute Available→Us → _The SURFACES/LABEL_MATCH/SOURCE_AUTHORITY omissions are literally real, but sourceConsumption.ts is entirely unwired dead code — the string "sourceConsumption" _
- `gemini.ts` — callLLM never reads finish_reason, so a max_tokens-clipped response is recorded ok:true in the health ring while the caller's catch silently swallows the result → _Code facts are accurate (finish_reason is never read; line 151 sets ok:true for any 200; callers swallow parse errors), but the P1 premise "truncation is invisi_
- `enrichment.ts` — normalizeNewUserInsights still maps the OLD n8n source keys, so on the current live payload 8 of its 10 legacy entries are silently dropped — buildLedger and th → _The stale key-map in normalizeNewUserInsights (enrichment.ts:658-666, reading profile/rfq/whatsapp_conversations/befisc/sign3/usersince) is real and its deriveE_
- `enrichment.ts` — A failed pull leaves lastRich/lastRaw/lastHealth/lastServerTrace holding the PREVIOUS pull's (other glid/tier) data, and consumers read the getters unconditiona → _The no-reset code exists, but the claimed "prior buyer/tier rendered as new" is unreachable: the standalone's glid is fixed from the ?profile= URL param (App.ts_
- `externalRun.ts` — Cross-validation fabricates World/OSINT's 'company' fact from the seed itself, so any web search that returns ANY results counts as an independent corroboration → _The circular seed-copy at line 112 is real, but the P1 harm is not reachable: extractFacts copies the buyer's own first-party seed.companyName (never a namesake_
- `buyerProfileModel.ts` — The GLADMIN Company-Details GST field reads a different, narrower source set than the BuyLead GST-Verified ribbon, so a GST-verified buyer can show 'GST: Not av → _Refuted: GLADMIN's gstinVal (buyerProfileModel.ts:369) reads gstin_union.primary/gstins[], and gstin_union is defined as "GSTIN union (Sign3 ⊕ IDfy ⊕ Befisc)" (_
- `BuyerProfileCard.tsx` — The GLADMIN card hardcodes a 'TrustSEAL Verified' plan tile for every buyer even though the model documents omitting the plan entirely because no plan data exis → _The hardcoded "TrustSEAL Verified" tile does exist at BuyerProfileCard.tsx:442-447, but it is an intentional, triple-disclaimed placeholder (dashed-border/gray _
- `gemini.ts` — The IndiaMART litellm gateway Bearer key (VITE_LLM_KEY) is compiled into the production JS bundle and into every downloadable offline HTML that users email arou → _Code facts reproduce (gemini.ts:12/:126 send the sk-g… gateway key client-side, /api/llm proxy injects no auth, key inlines 4x into dist/assets/index.js and pub_
- `extracttest.mjs` — extracttest asserts an ungrounded-confidence floor (≤30) as a HONESTY guarantee, but the live extract path removed that floor — the harness is green-but-lying a → _Code quotes are accurate (mirror has a 30-floor at line 31/103; real extractedToFinals+confidenceFinal have none), but the "data-honesty gap" premise is refuted_
- `RFQ Buyer Insights — v44 (syntax repair · namesake field-guard · no-audio skip) [bi-user-insights-v10x].json` — IDfy async ind_gst_certificate uses a single fixed wait then ONE poll with no retry loop (dashboard wait=6s, standalone wait=10s) — unlike calls/pns/websearch w → _REFUTED — the topology is exactly as described, but the load-bearing P1 impact ("cert-only fields never arrive / data lost every run") is false.

TOPOLOGY (conf_
- `RFQ Buyer Unified — v40 (syntax repair · namesake field-guard · no-audio skip) [bi-buyer-unified].json` — deal_readiness is missing from the standalone profile-bundle prompt but the card renders it — so even with the LLM chain fixed the standalone card can never sho → _REFUTED (behaves-differently). The data-existence facts check out — deal_readiness appears 0 times in the standalone n8n profile-bundle prompt (v40); its Valid-_